<div class="jumbotron">
    <div class="container">
        <h1>{{title}}</h1>
        <p>Create a JavaScript shim set. <br/>Also available as an NPM package <code>npm install shimly</code> or as a Grunt plugin <code>npm install grunt-shimly</code></p>
    </div>
</div>
<div class="container">
    <p>Please select shims to generate a shim set.</p>
    <br/>
    <form method="post" action="/shim">
        <div class="form-group">
        {{#shims}}
        <label class="checkbox-inline" style="width: 33%; margin-left: 0;">
            <input type="checkbox" name="shims" value="{{this}}"> {{this}}
        </label>
        {{/shims}}
        </div>
        <hr/>
        <div class="checkbox">
            <label>
                <input type="checkbox" name="minify" value="true"> Minify
            </label>
        </div>
        <br/>
        
        <button type="submit" class="btn btn-primary">Generate</button>
    </form>
</div>